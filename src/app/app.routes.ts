import { Routes } from '@angular/router';
import { RedditListComponent } from './pages/reddit-list/reddit-list.component';

export const routes: Routes = [{ path: '', component: RedditListComponent }];
